# components/
> L2 | Parent: /CLAUDE.md

All React components organized by domain.

## Subdirectories

home/    — Homepage client components (stateful UI with timers, animations, live data)
ui/      — Shared UI primitives (GlassPanel, TechBorder, ProgressBar) — server components, no state
content/ — Content page renderers (EntryList, EntryPage) — server components, no state

[PROTOCOL]: Update this file on any subdirectory change, then check parent CLAUDE.md
